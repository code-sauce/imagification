paper receipt Scan

To run on OSX, you need native library - tesseract, which can be brewed by:
```
brew install tesseract
```

Other python packages can be installed via:
```
sudo pip install -r requirements.txt
```
